# В репозитории — LF, независимо от того, на чём правили.
# Иначе файлы, отредактированные на Windows, приезжают к Linux-читателям с CR
# и ломают diff, а в CI на ubuntu — шебанги и сравнения строк.
* text=auto eol=lf

# Бинарное — не трогать
*.png binary
*.pdf binary
*.bin binary
